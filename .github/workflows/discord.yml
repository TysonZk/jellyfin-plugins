name: Discord Button — Build & Release

on:
  push:
    tags:
      - 'discord-v*'
  pull_request:
    paths:
      - 'Discord/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Build
        run: dotnet build Discord/src/Jellyfin.Plugin.Discord/Jellyfin.Plugin.Discord.csproj -c Release

      - uses: actions/upload-artifact@v4
        with:
          name: Jellyfin.Plugin.Discord
          path: Discord/src/Jellyfin.Plugin.Discord/bin/Release/net8.0/Jellyfin.Plugin.Discord.dll

  release:
    if: startsWith(github.ref, 'refs/tags/discord-v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Get version
        id: version
        run: echo "value=${GITHUB_REF#refs/tags/discord-v}" >> $GITHUB_OUTPUT

      - name: Build
        run: dotnet build Discord/src/Jellyfin.Plugin.Discord/Jellyfin.Plugin.Discord.csproj -c Release

      - name: Create zip
        run: |
          mkdir -p dist
          cp Discord/src/Jellyfin.Plugin.Discord/bin/Release/net8.0/Jellyfin.Plugin.Discord.dll dist/
          cd dist && zip Jellyfin.Plugin.Discord.zip Jellyfin.Plugin.Discord.dll

      - name: MD5
        id: checksum
        run: echo "md5=$(md5sum dist/Jellyfin.Plugin.Discord.zip | cut -d' ' -f1)" >> $GITHUB_OUTPUT

      - name: Update manifest.json
        run: |
          python3 - <<'PYEOF'
          import json, os
          from datetime import datetime, timezone

          version   = os.environ['VERSION']
          checksum  = os.environ['CHECKSUM']
          tag       = os.environ['TAG']
          repo      = os.environ['REPO']
          timestamp = datetime.now(timezone.utc).strftime('%Y-%m-%dT%H:%M:%SZ')
          source_url = f"https://github.com/{repo}/releases/download/{tag}/Jellyfin.Plugin.Discord.zip"

          with open('manifest.json') as f:
              manifest = json.load(f)

          plugin = next(p for p in manifest if p['guid'] == 'c3f19a72-5d84-4e6b-a017-3b9e21cf8401')
          plugin['versions'] = [v for v in plugin['versions'] if v['version'] != version]
          plugin['versions'].insert(0, {
              "version":   version,
              "changelog": "See release notes on GitHub.",
              "targetAbi": "10.10.0.0",
              "sourceUrl": source_url,
              "checksum":  checksum,
              "timestamp": timestamp
          })

          with open('manifest.json', 'w', encoding='utf-8') as f:
              json.dump(manifest, f, indent=2, ensure_ascii=False)
              f.write('\n')
          print(f"manifest updated — Discord v{version}")
          PYEOF
        env:
          VERSION:  ${{ steps.version.outputs.value }}
          CHECKSUM: ${{ steps.checksum.outputs.md5 }}
          TAG:      ${{ github.ref_name }}
          REPO:     ${{ github.repository }}

      - name: Commit manifest
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add manifest.json
          git diff --staged --quiet || git commit -m "chore: manifest Discord ${{ github.ref_name }}"
          git push origin HEAD:main

      - uses: softprops/action-gh-release@v2
        with:
          files: dist/Jellyfin.Plugin.Discord.zip
          generate_release_notes: true
